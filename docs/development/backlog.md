# 设计待办（Design Backlog）

这里记录下一轮设计修订的候选事项，不改变当前 v0.1.0-draft.12 的规范。
以下六项已纳入 [v0.2.0-draft.3](../design/v0.2.0/index.md)，尚未实现。
原验收条件保留；许可证仍待维护者决定，日历市场覆盖仍待验证。

## 配置与忽略规则的可读性

- [ ] 按用途组织 .gitignore、.env.example 和本地 .env。
  - 类型：开发体验与可维护性改进（Developer Experience / Maintainability）。
  - 状态：已纳入 v0.2.0-draft.3，待实现；纳入下一轮设计修订。
  - 问题：当前条目集中平铺，难以看出用途、必填项及配置之间的关系。
  - .gitignore 建议分组：Python 与构建产物、虚拟环境、工具缓存、测试覆盖率、凭据与本地配置、日志与报告、文档产物、benchmark 结果。
  - .env.example / .env 建议分组：Tushare 凭据、PostgreSQL 连接、请求与重试策略、增量/刷新策略、日志与报告、终端展示。
  - 每组添加简短注释；关键项注明用途、单位、默认值、允许值和是否必填，避免让用户从代码猜配置含义。
  - “结构化”优先采用注释分节及稳定排序，保持 dotenv 语法；是否进一步分文件或采用其他格式，留待实际需要再决定。
  - 本地 .env 含真实凭据：更新时保留用户现值，只补充缺项或整理注释；不得复制到文档、Git 或终端输出。
  - 明确测试配置与正式配置的边界，避免让用户误以为测试连接可以回落正式库。

### 验收条件

1. 用户能快速定位连接参数、下载策略和展示偏好，并区分必填项与可选默认值。
2. .env.example 与配置加载器的键名、默认值和单位一致；本地 .env 沿用相同分组。
3. 整理前后实际配置值、配置优先级及 .gitignore 匹配行为不变，尤其保留 .env.example 的例外规则。
4. 现有凭据不丢失、不泄露；文档示例只包含占位值。


## 面向读者身份的文档设计

- [ ] 按使用者、运维人员、贡献者和设计评审者的身份完善文档结构与写作规范。
  - 状态：已纳入 v0.2.0-draft.3，待实现；纳入后续文档改进。
  - README 和用户指南聚焦产品用途、安装、使用方法及影响使用的限制。
  - 开发提醒、内部验收待办、实现过程和维护者行动项归入开发文档、验收记录或 backlog，不混入面向用户的 README 等页面。
  - 审查首页、README、指南和参考页，清理读者无需承担的开发上下文；通过链接提供必要的深入入口。

### 验收条件

1. 各类文档明确目标读者，同一页面不混用用户操作指南与开发提醒。
2. README 和用户指南能独立帮助用户完成操作，不要求理解开发进度或内部验收流程。
3. 必要的用户可见限制仍准确披露，内部提醒迁移后不丢失。

## 英文输入输出与界面布局

- [ ] 将软件面向用户的输入输出语言统一为英文，并优化终端界面布局；设计文档保持中文。
  - 状态：已纳入 v0.2.0-draft.3，待实现；纳入后续软件交互改进。
  - 范围包括输入提示、确认交互、错误、警告、进度、ETA、摘要及生成的报告等面向用户的文本。
  - 统一术语、日期/数字/单位和措辞；不翻译或改写 API 原始数据值。
  - 优化信息层次、对齐、换行和留白，检查窄终端、长字段、Rich/plain 与非交互输出。
  - 评估日志文本、结构化字段及现有命令兼容性，明确迁移范围；不为翻译擅自改变机器字段或命令语义。

### 验收条件

1. 软件交互和生成的人类可读输出无遗漏的中文提示，术语一致；设计正文仍为中文。
2. 常见终端宽度与 plain 模式下，关键信息可读，无截断或重叠。
3. 计数、退出码、请求与写入行为保持正确，布局调整不改变业务语义。

## CLI 帮助页界面设计

- [ ] 完善主命令与各子命令的帮助页布局和信息组织。
  - 状态：已纳入 v0.2.0-draft.3，待实现；与英文输出改进协调实施。
  - 按用户目的组织命令，清晰展示缩写、全局选项位置、必填参数、默认值和约束。
  - 提供简短、可执行的典型示例，解释 fetch、refresh、update 的使用区别。
  - 帮助页聚焦当前命令的操作，完整配置与背景说明链接至相应文档，避免堆叠大段设计说明。
  - 检查主帮助、子命令帮助、参数错误后的用法提示在窄屏及 plain 模式下的一致性。

### 验收条件

1. 用户能从帮助页判断该选哪个命令，以及如何正确组合参数与全局选项。
2. 示例、缩写、默认值和参数约束与实际 CLI 一致。
3. 帮助文本使用英文，排版清晰；查看帮助无需 Token、数据库或网络连接。


## 常规无交易日请求过滤

- [ ] 设计基于交易日历的请求过滤机制，减少周末、节假日等常规无交易日的无效请求。
  - 状态：已纳入 v0.2.0-draft.3，待实现；不改变当前请求规划行为。
  - 按 API 的数据语义和所属市场决定是否适用；不将所有时间型接口都视为仅交易日产生数据，也不只按星期几硬编码跳过。
  - 明确交易日历来源、缓存与更新策略，以及与固定块规划、fetch/refresh/update 的关系。
  - 日历缺失、过期或不可用时采用保守策略，不能仅凭未知日历将日期判为无交易日。
  - 保留必要的显式绕过入口，以应对日历修正和特殊数据核对。
  - 在计划与报告中区分“无交易日过滤”“本地有效记录跳过”和“请求成功但为空”，不把未请求日期记作远端请求成功。

### 验收条件

1. 适用接口的周末和节假日请求能被合理过滤，跨市场或不适用接口不会被误跳过。
2. 覆盖连续休市、节前节后、跨年、日历缺失/过期/修正和显式绕过的测试场景。
3. 过滤后的块规划、覆盖说明和统计口径一致，不将过滤结果当作业务完整性证明。
4. benchmark 比较实际请求次数与耗时，验证节省请求的效果及日历查询本身的成本。


## 开源许可证选择

- [ ] 选择适合项目的开源 License。
  - 状态：待评估、待决定；本条不代表已选定或授予任何许可证。
  - 明确对商用、修改、再分发和衍生项目的授权意图，比较合适的许可证及其义务。
  - 核对依赖许可证兼容性；区分项目代码授权与 Tushare 数据、接口使用条款，不将代码许可证解释为数据再分发授权。

### 验收条件

1. 记录最终许可证选择及理由，由维护者确认。
2. 决定后添加正式 LICENSE 文件，同步 pyproject.toml、README 及需要的版权声明，保持一致。
